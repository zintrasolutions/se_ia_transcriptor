# Configuration de performance pour la transcription
# Copiez ce fichier vers .env pour l'utiliser

# ===== CONFIGURATION WHISPER =====

# Modèle Whisper (du plus rapide au plus précis)
# Options: tiny, base, small, medium, large
WHISPER_MODEL=tiny

# Nombre de threads CPU pour Whisper
# Recommandé: 4-8 pour la plupart des systèmes
WHISPER_NUM_THREADS=4

# Taille du batch pour le traitement
# Recommandé: 16-32 pour CPU, 64-128 pour GPU
WHISPER_BATCH_SIZE=16

# Langue par défaut pour la transcription
WHISPER_LANGUAGE=en

# ===== OPTIMISATIONS NODE.JS =====

# Taille du pool de threads Node.js
UV_THREADPOOL_SIZE=16

# Mémoire maximale pour Node.js (en MB)
NODE_OPTIONS=--max-old-space-size=4096

# ===== CONFIGURATION DOCKER =====

# Limites de ressources Docker
# CPU: nombre de cœurs (ex: 4.0 = 4 cœurs)
# Mémoire: en GB (ex: 8G = 8 GB)

# Pour une transcription rapide (modèle tiny)
# DOCKER_CPU_LIMIT=2.0
# DOCKER_MEMORY_LIMIT=4G

# Pour une transcription équilibrée (modèle base)
# DOCKER_CPU_LIMIT=4.0
# DOCKER_MEMORY_LIMIT=8G

# Pour une transcription précise (modèle large)
# DOCKER_CPU_LIMIT=8.0
# DOCKER_MEMORY_LIMIT=16G

# ===== PROFILS DE PERFORMANCE =====

# Profil RAPIDE (vitesse maximale)
# WHISPER_MODEL=tiny
# WHISPER_NUM_THREADS=4
# WHISPER_BATCH_SIZE=16

# Profil ÉQUILIBRÉ (bon compromis)
# WHISPER_MODEL=base
# WHISPER_NUM_THREADS=6
# WHISPER_BATCH_SIZE=24

# Profil PRÉCIS (qualité maximale)
# WHISPER_MODEL=large
# WHISPER_NUM_THREADS=8
# WHISPER_BATCH_SIZE=32
