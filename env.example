# Configuration Privacy IA Local Transcriptor


# Port du serveur (optionnel, défaut: 3001)
PORT=3001

# Configuration Ollama (optionnel, défaut: http://localhost:11434)
OLLAMA_BASE_URL=http://localhost:11434

# Modèle Ollama à utiliser (optionnel, défaut: llama3.1)
OLLAMA_MODEL=llama3.1

# Configuration Whisper local (optionnel)
WHISPER_MODEL=base
WHISPER_LANGUAGE=en
